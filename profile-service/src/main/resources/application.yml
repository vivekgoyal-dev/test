server:
  port: 9081
spring:
  application:
    name: profile-service
  datasource:
    url: jdbc:h2:file:./data/profiledb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
