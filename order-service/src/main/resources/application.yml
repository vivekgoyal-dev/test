server:
  port: 9084
spring:
  application: 
    name: order-service
  datasource:
    url: jdbc:h2:file:./data/orderdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
