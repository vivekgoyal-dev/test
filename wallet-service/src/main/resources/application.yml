server:
  port: 9085
spring:
  application:
    name: wallet-service
  datasource:
    url: jdbc:h2:file:./data/walletdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
